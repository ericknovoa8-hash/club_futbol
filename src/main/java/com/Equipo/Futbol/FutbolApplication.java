package com.Equipo.Futbol;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
/**
 * En esta clase se configura la aplicación de Spring Boot y se define el punto de 
 * entrada para ejecutar la aplicación.
 */
public class FutbolApplication {
    public static void main(String[] args) {
        SpringApplication.run(FutbolApplication.class, args);
    }
    
}
